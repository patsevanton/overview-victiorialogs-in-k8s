## Статистика

Статистику по выбранным логам можно рассчитать с помощью конвейера ``stats``.

Также возможно выполнять статистические вычисления для `выбранных записей логов` на стороне клиента — с использованием команд Unix `sort`, `uniq` и других, согласно `этой документации`.

